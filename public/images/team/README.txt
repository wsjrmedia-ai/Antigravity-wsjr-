Save the CEO / founder portrait here as:

    vishnu-das.jpg

The WhoWeArePage Leadership section references this path:
    /images/team/vishnu-das.jpg

Recommended:
 - Portrait orientation (the page layout expects ~2:3 aspect ratio)
 - 1200–1600px wide, JPEG quality 85, under ~400KB if possible
 - Keep face roughly centred horizontally in the upper third

If the file is missing, the founder card falls back to a graceful
placeholder block so the page still renders cleanly.
